class Env {
  static const String apiUrl = "http://10.0.2.2:3000/api/v1"; // Default for Android Emulator pointing to localhost
  // In a real app, this would be loaded from --dart-define or a .env file
}
