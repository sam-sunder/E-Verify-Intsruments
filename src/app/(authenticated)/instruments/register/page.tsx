import { RegisterInstrument } from "@/components/business-owner/register-instrument";

export const metadata = {
  title: "Register Instrument | e-VerifyMet",
  description: "Register a regulated weighing or measuring instrument",
};

export default function RegisterInstrumentPage() {
  return <RegisterInstrument />;
}
